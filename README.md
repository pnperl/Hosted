# Hosted
Web page sharing

## Data source

The app uses Google News RSS search feeds instead of NewsAPI, which avoids API key setup and rate-limit issues.

The visualization is **live-data only**. If Google News RSS is unavailable, the app shows an error state instead of synthetic/mock data.
